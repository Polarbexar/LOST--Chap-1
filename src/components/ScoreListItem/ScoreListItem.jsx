

export default function ScoreListItem({score}) {
  return (
  <li>{score.name} - {score.highScore} points</li>
)}
  
  //   <div>
  //   {score.PersonalScoreboard ? (
  //     <li>{score.scores.score} </li>
  //     ) : ( <li>{score.name} : {score.highScore} points</li>
      
      
  //   )}
  // </div>
  // )}